@model EmployeeMVC.Models.ViewModels.HomeViewModel

@foreach(var notification in Model.Notifications){

    <div class="notification @(notification.IsSeen?"read":"unread")">@notification.Message-@notification.CreatedAt.ToString("yyyy-MM-dd HH:mm:ss")</div>
}